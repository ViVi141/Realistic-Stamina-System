# docs/workshop

Workshop 发布用历史文案（changenote / 英文描述）。  
最新游戏内版本以根目录 [CHANGELOG.md](../../CHANGELOG.md) 与 `SCR_RSS_ConfigManager` 为准；此处文件可能落后于代码。

发布新版时：从 CHANGELOG 顶部摘一段写入新的 `WORKSHOP_CHANGENOTE_vX.Y.Z.txt`，勿再堆回仓库根目录。
